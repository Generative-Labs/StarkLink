import { Client } from '../client';
import { PageParams, TopicListType, SubscribeListType } from '../types';
export declare class Topic {
    private readonly _client;
    private readonly _keys;
    myTopicList: TopicListType[] | null;
    mySubscribeList: SubscribeListType[] | null;
    constructor(client: Client);
    getMyCreateTopicList(options: PageParams): Promise<any>;
    getMySubscribeTopicList(options: PageParams): Promise<any>;
    createTopic(topic_name: string): Promise<any>;
    subscribeTopic(topicid: string): Promise<import("axios").AxiosResponse<any, any>>;
    publishTopicMessage(params: {
        topicid: string;
        title: string;
        content: string;
    }): Promise<void>;
}
