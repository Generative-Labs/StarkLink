import { Client } from '../client';
import { PageParams, MessageStatus, MessageListItem } from '../types';
export declare class Message {
    private readonly _client;
    private readonly _keys;
    msg_text: string;
    messageList: MessageListItem[] | null;
    constructor(client: Client);
    getMessageList(option: PageParams, userId?: string): Promise<void>;
    /**
     * if message from group chat: topic = group id
     * if message from one chat: topic = userid
     */
    changeMessageStatus(messages: string[], status?: MessageStatus): Promise<import("axios").AxiosResponse<any, any> | undefined>;
    sendMessage(msg: string, userId?: string): Promise<void>;
    receive: (pbType: number, bytes: Uint8Array) => Promise<void>;
}
