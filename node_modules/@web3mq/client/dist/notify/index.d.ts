import { Client } from '../client';
import { MessageStatus, NotifyResponse } from '../types';
export declare class Notify {
    private readonly _client;
    private readonly _keys;
    notificationList: NotifyResponse[] | null;
    constructor(client: Client);
    changeNotificationStatus(messages: string[], status?: MessageStatus): Promise<import("axios").AxiosResponse<any, any>>;
    receiveNotify: (pbType: number, bytes: Uint8Array) => void;
}
