export * from './signConnect';
export * from './dappConnect';
import { Client } from '../client';
import { WebsocketPingCommand } from '../pb';
export declare class Connect {
    private _client;
    private timeout;
    private timeoutObj;
    ws: WebSocket | null;
    nodeId: string;
    constructor(client: Client);
    init(): void;
    onMessageCallback(PbType: number, bytes: Uint8Array): WebsocketPingCommand | undefined;
    sendPing(): void;
    reset(): void;
    start(): void;
    send(arr: Uint8Array): void;
    receive(pbType: number, bytes?: Uint8Array): void;
    receiveNotify(pbType: number, bytes?: Uint8Array): void;
}
